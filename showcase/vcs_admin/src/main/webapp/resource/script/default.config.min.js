var backdrop = $("<div>").addClass("modal-backdrop absolute fade in");
			
var mask = function(){
	$(config.target).append(backdrop);
},
unmask = function() {
	backdrop.remove();
};



$(document).ajaxComplete(function(){

	$(".collapse").collapse();
	
	$(".modal-backdrop").remove();
	
	$("form").validate({
		submitHandler : function(f) {
			form.ajaxSubmit(/*{
				beforeSubmit:beforeSubmit,
				success:function(response, status, options, form) {
					success({
						response:response,
						status:status,
						options:options,
						form:form
					});
				}
			}*/);
		}
	});

});

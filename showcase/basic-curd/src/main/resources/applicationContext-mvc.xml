<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc 
		http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/aop 
		http://www.springframework.org/schema/aop/spring-aop.xsd 
		http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context.xsd">

	<description>Spring MVC 3配置 </description>
	
	<!-- 定义受环境影响易变的变量 -->
	<context:property-placeholder ignore-resource-not-found="true" location="classpath*:/application.properties" />

	<aop:aspectj-autoproxy proxy-target-class="true" />

	<!-- 自动扫描且只扫描@Controller -->
	<context:component-scan base-package="org.exitsoft.showcase.web" use-default-filters="false">
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller" />
	</context:component-scan>

	<mvc:view-controller path="/" view-name="redirect:/index" />

	<!-- 使用缓存mvc注解配置 -->
	<mvc:annotation-driven>
		<!-- 配置StringHttpMessageConverter当@ResponseBody 时中文正常显示 -->
		<mvc:message-converters>
			<bean class="org.springframework.http.converter.StringHttpMessageConverter">
				<property name="supportedMediaTypes">
					<list>
						<value>text/plain;charset=UTF-8</value>
					</list>
				</property>
			</bean>
		</mvc:message-converters>
	</mvc:annotation-driven>
	
	<!-- 使用自定拦截器来做操作记录支持 -->
	<mvc:interceptors>
		<mvc:interceptor>    
	        <bean class="org.exitsoft.showcase.service.foundation.OperatingRecordInterceptor"/>  
	    </mvc:interceptor>
    </mvc:interceptors>

	<!-- spring mvc 的资源路径 -->
	<mvc:resources mapping="/resource/**" location="/resource/" />

	<!-- 使用默认的servlet handler -->
	<mvc:default-servlet-handler />
	
	<!-- 启动文件上传Resolver -->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver" />
	
	<!-- 启动异常Resolver -->
	<bean class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
		<property name="exceptionMappings">
			<map>
			  <entry key="org.exitsoft.showcase.service.ServiceException" value="error/service-exception" />
			</map>
		</property>
	</bean>
	
	<!-- 定义块，可以被子模板用@override指令覆盖显示,没有被override则显示自身的内容 -->
	<bean id="blockDirective" class="org.exitsoft.common.freemarker.directive.BlockDirective"/>
	<!-- 继承其它模板，必须放在模板的最后面 -->
    <bean id="extendsDirective" class="org.exitsoft.common.freemarker.directive.ExtendsDirective"/>
    <!-- 重定义@block指令显示的内容 -->
    <bean id="overrideDirective" class="org.exitsoft.common.freemarker.directive.OverrideDirective"/>
    <!-- 在override中显示 super block的内容,类似java的 super关键字 -->
    <bean id="superDirective" class="org.exitsoft.common.freemarker.directive.SuperDirective" />

	<!-- freemarker的格式和其他配置 -->
	<bean id="freemarkerConfigurer" class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
		<property name="templateLoaderPath" value="/WEB-INF/page/" />
		<property name="defaultEncoding" value="UTF-8" />
		<property name="freemarkerSettings">
			<props>
				<prop key="template_update_delay">10</prop>
				<prop key="locale">zh_CN</prop>
				<prop key="datetime_format">yyyy-MM-dd HH:mm:ss</prop>
				<prop key="date_format">yyyy-MM-dd</prop>
				<prop key="number_format">#.##</prop>
			</props>
		</property>
		<!-- 将extends override block super指令配置到freemarker中，实现freemarker页面继承功能-->
		<property name="freemarkerVariables">
			<map>
				<entry key="extends" value-ref="extendsDirective" />
				<entry key="override" value-ref="overrideDirective" />
				<entry key="block" value-ref="blockDirective" />
				<entry key="super" value-ref="superDirective" />
			</map>
		</property>
	</bean>

	<!-- FreeMarker视图解析,在这里配置后缀名ftl和视图解析器 -->
	<bean id="viewResolver" class="org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver">
		<property name="suffix" value=".html" />
		<property name="requestContextAttribute" value="request" />
		<property name="contentType" value="text/html;charset=UTF-8" />
	</bean>
	
   	<!-- 启用shrio授权注解拦截方式  -->
	<bean class="org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor">
	    <property name="securityManager" ref="securityManager" />
	</bean>

</beans>
